# MLops-hw

В examples есть питоновский код который можно просто через командную строку вызывать, тестил до стримлита

стримлит запускается через streamlit run dashboard/app.py
поетри через poetry run python app/main.py